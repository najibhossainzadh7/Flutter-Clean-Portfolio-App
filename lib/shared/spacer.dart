import 'package:flutter/material.dart';

class HeightGap extends SizedBox {
  const HeightGap(double size, {super.key}) : super(height: size);
}

class WidthGap extends SizedBox {
  const WidthGap(double size, {super.key}) : super(width: size);
}
